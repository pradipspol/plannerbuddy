# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build planner-buddy-backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t planner-buddy-backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system planner-buddy-backend && \
          adduser --system -G planner-buddy-backend planner-buddy-backend

COPY apps/planner-buddy-backend/dist planner-buddy-backend/
RUN chown -R planner-buddy-backend:planner-buddy-backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix planner-buddy-backend --omit=dev -f install

CMD [ "node", "planner-buddy-backend" ]
